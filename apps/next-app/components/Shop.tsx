import { Flex, Text } from "@chakra-ui/react";
import { useState } from "react";

const Shop = () => {
  const [balance, setBalance] = useState();
  return (
    <Flex>
      <Text>Shop</Text>
    </Flex>
  );
};
export default Shop;
